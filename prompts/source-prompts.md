# Invocation Pattern

Use `docs/source-policy.md` and `skills/source-evaluator/SKILL.md`. Evaluate each source independently and map it to Claim IDs. Do not grade reputation instead of direct support.
